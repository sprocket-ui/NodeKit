prepare-commit-msg:
  commands:
    commitzen:
      interactive: true
      run: pnpm run cz --hook # Or npx cz --hook
      env:
        LEFTHOOK: 0

commit-msg:
  commands:
    "lint commit message":
      run: pnpm run commitlint --edit {1}
