name: Release

on:
  workflow_run:
    workflows: ["Test"]
    types: [completed]
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

env:
  CI: true
  LEFTHOOK: 0

jobs:
  build:
    name: Build
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/build.yml

  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write

    steps:
      - name: Generate GitHub App token
        id: generate_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.SPROCKET_BOT_ID }}
          private-key: ${{ secrets.SPROCKET_BOT_KEY }}

      - name: Checkout Repo
        uses: actions/checkout@v6
        with:
          token: ${{ steps.generate_token.outputs.token }}

      - name: Setup pnpm + install deps
        uses: pnpm/action-setup@v5
        with:
          run_install: |
            - args: [--frozen-lockfile, --ignore-scripts]

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact-name }}
          path: packages

      - name: Strip devDependencies from packages
        uses: actions/github-script@v9
        with:
          github-token: ${{ steps.generate_token.outputs.token }}
          script: await import(`${process.env.GITHUB_WORKSPACE}/scripts/clean-publish.mjs`)

      - name: Create Release PR
        id: create_release_pr
        uses: changesets/action@v1
        with:
          title: 'chore: release packages'
          createGithubReleases: true
          commit: 'chore: package versioning'
          publish: pnpm exec changeset publish
        env:
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
          NPM_TOKEN: ''
          NPM_CONFIG_PROVENANCE: true
          NPM_CONFIG_ACCESS: public

      - name: Enable auto-merge on release PR
        if: steps.create_release_pr.outputs.pullRequestNumber
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ steps.generate_token.outputs.token }}
          pull-request-number: ${{ steps.create_release_pr.outputs.pullRequestNumber }}
          merge-method: squash
