# Automatically resolve workspace protocol to package versions when publishing
publish-branch=main
link-workspace-packages=true

# This tells pnpm to replace "workspace:*" with actual versions when publishing
